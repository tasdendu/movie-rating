{
  "name": "movie-rating",
  "version": "1.0.0",
  "description": "",
  "main": "truffle.js",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "start": "lite-server",
    "dev": "lite-server",
    "test": "lite-server",
    "test1": "echo \"Error: no test specified\" && exit 1",
    "build": "truffle compile && truffle migrate && lite-server"
  },
  "author": "Tashi",
  "license": "ISC",
  "devDependencies": {
    "@truffle/hdwallet-provider": "^1.2.0"
  },
  "dependencies": {
    "lite-server": "^2.6.1",
    "truffle": "^5.11.2"
  }
}
